```python
# f-string 写法
print(f'李四的体重是 {weight:.1f} 公斤') # 我的这里不是花括号吗？怎么打印出来的而不是我想要的结果

# 比较麻烦的写法：在 Python 里，逗号 = 参数分隔符
print(f'我的体重是',weight,'公斤')

# 错误的写法，这里的{weight}变量被当成是一个集合了
print(f'我的体重是',{weight},'公斤')
```
